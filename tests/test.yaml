.services:
  jira: &services_jira
    active: true
    commit_events: false
    merge_requests_events: true
    url: https://example.net
    username: "${JIRA_USER_NAME}"
    password: "${JIRA_USER_PASSWORD}"
    ignore: "$JIRA_USER_NAME"
    jira_issue_transition_id: 61
    both_stuff: "${JIRA_USER_NAME}"+"${JIRA_USER_PASSWORD}"
